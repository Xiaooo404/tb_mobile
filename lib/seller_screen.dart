
import 'package:flutter/material.dart';

class SellerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Manage Sellers'),
      ),
      body: Center(
        child: Text('Seller Management UI Here'),
      ),
    );
  }
}